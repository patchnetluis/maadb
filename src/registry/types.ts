// ============================================================================
// Registry Types — re-exported from core types for module-local use
// ============================================================================

export type {
  Registry,
  RegistryType,
  ExtractionConfig,
  DocType,
  SchemaRef,
  FilePath,
  Primitive,
} from '../types.js';
